# DAIKUAN 系统记忆与施工准则

最后更新：2026-06-02

## 1. 系统目标

本系统要做成一个可用、能用、专业的贷款平台，而不是演示系统。核心是三端无缝闭环：

- 管理端：管理员和工作人员处理客户、产品、审批、放款、还款、逾期、资金方、财务、权限和审计。
- 客户端：客户自己完善资料、上传证件、申请借款、签署合同、查看实时借款时间与应还金额、提交还款确认。
- 资金端：资金方查看资金账户、入金、已放出本金、收益到期、平台打款、确认到账、反馈未收到、提现和对账单。

## 2. 三端闭环原则

任何核心业务都必须形成闭环，不允许只在一个端展示、另一个端不知道。

- 客户资料闭环：客户强制填写地址、电话、税号、身份证、护照、居留信息，并上传复印件；管理端审核和查看；资料不完整时不能确认放款。
- 借款合同闭环：客户在客户端签名一次，签名落到借款合同；合同随借款有效，还款结清后自动失效。
- 放款闭环：管理端确认放款，客户确认收到款，资金账户扣减并写资金流水。
- 还款闭环：客户点还款按钮后，暂停额外利息显示；管理端确认收到金额则入账；如果管理端点未收款，客户端恢复计时并把暂停期间也计入。
- 资金方收益闭环：系统生成收益结算单，管理端登记平台打款，资金方确认到账后进入资金方财务；资金方点未收到则退回待核对状态。
- 对账闭环：平台总账、客户流水、资金账户流水、资金方收益结算单必须能互相追溯。

## 3. 关键业务规则

客户侧计息规则：

- 当天晚上拿款、当天晚上或凌晨还款，按 2% 收取利息。
- 第二天下午或第二天晚上还款，按 3% 收取利息。
- 按周借款时，以放款时间为基准计算周周期；到第 7 天同一时间点前还款按本周，到点以后自动登记逾期并追加逾期费用。
- 特殊客户可以有独立周息，不固定为 5%；管理端客户详情的专属定价是客户侧特殊费率入口。

资金方侧收益规则：

- `FIXED_MONTHLY`：固定月息，使用 `Funder.monthlyRate`，默认可为 2%，也可按沟通调整为 1% 或其他；按实际日历月周期计算，2月为28/29天、6月为30天、7月为31天，提前结清的零头按所在日历月实际天数折算。
- `VOLUME_BASED`：固定周息，使用 `Funder.weeklyRate`，默认可为 1.5%，也可按沟通调整为其他周息。
- `PROFIT_SHARE`：按实际收益分润，使用 `Funder.profitShareRatio`，例如 `0.3` 表示客户已确认收益的 30%。
- 管理端是资金方利率和分润规则的配置入口；资金端只展示真实规则、收益、结算状态和确认动作。

## 4. 单一事实源

- 数据模型：`prisma/schema.prisma`
- 客户侧计息引擎：`src/lib/interest-engine.ts`
- 客户特殊周息：`Customer.weeklyInterestRateOverride`
- 产品定价规则：`PricingRule`
- 资金方合作模式和展示：`src/lib/funder-cooperation.ts`
- 资金方到期收益结算：`src/services/funder-interest-settlement.service.ts`
- 资金方可提现与预估收益：`src/services/funder-interest.service.ts`
- 平台财务结算报表：`src/services/settlement.service.ts`
- 状态集合：`src/lib/business-status.ts`
- 权限和菜单：`permissions` / `role_permissions` / `src/lib/rbac.ts`

禁止在页面里重新硬编码资金方模式、客户状态、资金流水状态。新增状态或模式时先改单一事实源，再让页面引用。

## 5. 已完成的重要改造

- 三端基础结构：管理端 `/admin/*`、客户端 `/client/*`、资金端 `/funder/*`。
- 客户资料/KYC：地址、电话、税号、身份证、护照、居留信息和证件上传；资料不完整拦截放款。
- 客户签名合同：客户端签名动作，签名落合同；借款结清后合同失效。
- 实时还款链：客户端还款按钮、暂停额外利息、管理端确认收款/未收款联动。
- 资金方收益结算：到期结算单、平台打款登记、资金方确认到账/反馈未收到、通知和 CSV 导出。
- 资金方对账：资金账户流水、收益入账、提现出账、对账单导出。
- 财务一致性：统一总账、资金账户流水、收益结算单、幂等键、重复提交保护。
- 数据清理：业务演示数据已清空，保留干净系统基础账号、角色、权限、产品、模板和设置。
- 安全基础：公开表已启用 RLS 的批量脚本 `scripts/enable-public-rls.js`，命令为 `npm run db:enable-rls`。

## 6. 本轮新增/强化

- 新增 `src/lib/funder-cooperation.ts`，统一资金方三种合作模式、标签、费率展示、规则说明和周期判断。
- 新增 `src/lib/calendar-period.ts`，统一自然月周期，避免月息继续按固定30天计算。
- 强化 `src/services/schedule.service.ts`，按天借款的到期日使用真实期限天数，长天数分期按实际每期天数计息，一次性还本付息按完整期限计息，避免 7 天借款被错误排成 1 天到期。
- 管理端资金方新增/编辑支持 `PROFIT_SHARE`，并校验分润比例。
- 月息/周息模式保存时自动清理分润比例，避免旧比例误参与计算。
- 资金端仪表盘、提现页、合同页使用统一合作规则展示。
- 资金方合同模板新增“按实际收益分润模式”。
- 资金方收益结算生成按真实规则写入 `ruleMode`，分润模式利率展示为百分比口径。
- 资金方可提现收益、预计收益、平台财务结算报表同步支持分润模式。

## 7. 审计结论

使用 CRM Audit 七角色视角后的核心判断：

- PM：三端方向正确，但任何资金和还款动作必须有终态和反向处理，不允许只做确认不做未收到/驳回。
- UX：资金方看到的规则必须是业务语言，不应出现内部枚举或含糊的“业务量”。
- 架构：资金方模式以前分散在页面、API、服务和合同模板里，必须统一到 `src/lib/funder-cooperation.ts`。
- 技术负责人：核心金额链必须保持幂等，确认、驳回、提现、结算生成都要防重复。
- DBA：金额使用 Decimal，业务状态和模式虽为字符串，也必须通过共享常量/工具约束。
- QA/审计：所有资金动作要能审计、导出、追溯，未收到/未收款路径必须继续计算或回到待处理。
- 财务运营：资金方的本金、收益、提现、确认入账要能形成财务台账，让资金方知道自己赚了多少。

## 8. 目标架构蓝图

管理端是配置和审核中心，客户端是客户自助履约中心，资金端是资金方收益和对账中心。三端不直接互相写对方数据，统一通过 API、服务层、资金流水和通知系统联动。

核心原则：

- 后台配置，前台展示；前台确认，后台核对；资金变动必须写账。
- 每个业务动作都要有状态、操作人、时间、可重放保护和审计线索。
- 所有利率和合作模式只从数据库与共享规则工具读取。
- 合同、计划、结算单要保留规则快照，避免后续调费率影响历史口径。

## 9. 执行计划

优先级 P0：

- 保持三端主流程可跑通：客户资料完成 -> 申请 -> 审批 -> 签约 -> 放款 -> 客户确认 -> 还款 -> 管理端确认 -> 结清。
- 保持资金方主流程可跑通：入金 -> 管理端确认到账 -> 放款使用资金 -> 收益结算 -> 平台打款 -> 资金方确认 -> 对账。
- 所有金额相关 API 保留幂等保护和权限检查。

优先级 P1：

- 为资金方合作规则、客户特殊定价、还款冻结/恢复增加回归测试。
- 继续把页面里的旧模式标签迁移到共享规则工具。
- 将资金方合同签署和资金方入金凭证确认做得更正式。

优先级 P2：

- 强化移动端体验、空状态、错误提示和对账下载。
- 为管理端增加“流程体检”工作台，显示资料缺失、逾期、待收款、待资金方确认等异常。

## 10. 常用命令

- `npm run build`：生产构建和 Prisma 生成。
- `npm test`：单元测试。
- `npm run lint`：Next lint。
- `npm run db:seed`：基础种子数据。
- `npm run db:clear-business-data`：清理业务演示数据，保留干净系统。
- `npm run db:enable-rls`：为公开表启用 RLS。
- `npm run test:finance-reconciliation`：财务一致性测试。
- `npm run test:launch-readiness`：上线可用性冒烟测试。

## 11. Git 与发布纪律

- 用户明确要求过：前期构建好一定要 BLTDU，不要直接同步 GitHub。
- 同步 GitHub 前必须先审计、跑测试/构建，并确认没有明显问题。
- 不删除用户未要求删除的数据，不回滚用户改动。
- 如果业务数据只是演示数据，按用户要求清理成干净系统。

## 12. 2026-06-02 继续落刀记录

- 客户端自助还款被明确为“全额结清申报”动作：只有提交当前全部应还金额时，才允许暂停整笔借款新增利息。
- 部分还款不允许通过客户端自助入口冻结整笔借款利息，避免客户少量付款后整笔停止计息；部分还款应由管理端人工登记、分配和核销。
- 新增 `src/lib/repayment-runtime.ts` 的 `isFullPayoffAmount` 作为单一判断入口，并由客户端还款 API 使用。
- 客户端还款表单展示为本次结清金额，只读显示当前应还，减少误操作和争议。
- 管理端还款分配必须做到“还款单金额 = 分配总额”，否则不得进入客户确认和财务入账；避免资金账户入账金额与还款计划扣减金额不一致。
- 管理端选择待分配还款单时必须重新加载该计划期次，不能沿用上一个计划的旧期次状态。
- 还款暂停计息必须同时满足“流程状态仍在处理中”和“存在明确冻结快照 `interestFrozenAt` + `frozenPayableAmount`”；状态本身不等于冻结。
- 管理端人工登记/分配的部分还款不得自动冻结整笔借款实时利息；只有客户端自助全额结清申报生成的明确冻结快照才会暂停新增利息。
- 还款分配不仅总额要相等，还必须按本金、利息、费用、罚息分别受科目上限约束；不得把利息/费用误分到本金，否则平台利润、资金账户利润和资金方分润都会失真。
- 管理端还款分配默认应根据期次结构自动拆分：费用、利息、本金，超出计划结构的实时逾期部分归罚息；业务员可调整但后端必须校验科目上限。
- 还款分配科目、标签、上限校验的单一规则源是 `src/lib/repayment-allocation.ts`；API 和管理端页面都应引用它，不能各自硬编码。
- 管理端人工登记还款允许部分还款，但登记金额不得超过该计划当前实时可还余额；单期计划按 `calculateLiveOutstandingFromSnapshot` 重新计算实时应还，多期计划按开放期次 `remaining` 汇总。规则源是 `src/lib/repayment-registration.ts`，后台表单和 `/api/repayments` 都要引用它。
- 资金方收益结算确认链路必须并发安全：`fund_account_journal` 使用 `referenceType + referenceId` 唯一约束作为资金账户流水幂等键；资金方确认到账只允许从 `PAID_BY_PLATFORM` 进入 `CONFIRMED_BY_FUNDER` 并写入一条收益流水，重复确认只返回既有结果；资金方反馈未收到只能从 `PAID_BY_PLATFORM` 进入 `FUNDER_REJECTED`，不能覆盖已确认入账的收益单。
- 资金账户流水的 `balanceBefore/balanceAfter` 必须以资金账户当前余额为准，并在事务内通过 `SELECT ... FOR UPDATE` 锁定 `fund_accounts` 行后写入；不要再用“上一条流水余额”推导当前余额，避免同一资金账户并发入金/放款/还款/提现时流水余额错位。
- 客户台账 `ledger_entries` 使用 `type + referenceType + referenceId` 唯一约束防重复入账；写客户台账时如有 `customerId` 必须先锁定 `customers` 行再读取上一条台账余额。逾期日利息支付的台账引用必须包含日期，格式由 `src/lib/ledger-reference.ts` 的 `buildOverdueInterestLedgerReferenceId(overdueRecordId, date)` 生成，避免同一天重复入账，也避免不同日期并发覆盖 `paidDates`。
- 资金方提现审批必须把“资金方审批链锁定、冷却期复查、候选账户逐个锁定、扣款流水、账户余额更新、提现单账户落点”放在同一个事务里。指定账户只代表优先尝试，若并发后余额不足，应自动尝试同资金方其他可用账户；所有账户都不足时才失败，避免旧余额误判。
- 客户台账和全局总账的查询/导出排序必须使用 `createdAt + id` 稳定排序；合并客户台账与资金账户流水时也要有确定性兜底，避免同一时间多笔流水导致翻页或 CSV 审计顺序抖动。
- 资金账户余额、`totalInflow`、`totalOutflow`、`totalProfit` 的变动必须从 `src/services/fund-account-ledger.service.ts` 的账务 helper 发起；入金确认、入金撤销、放款出账、资金方收益入账、资金方提现都要在同一个事务锁内写资金账户流水并把账户 `balance` 设置为该流水的 `balanceAfter`。业务 API 不应再手写“写流水后单独 increment/decrement 账户余额”的散点逻辑。
- 客户还款到账确认也属于资金账户主链：`src/lib/repayment-confirm.ts` 必须通过 `writeFundAccountLedgerEntryAndUpdateAccount` 写 `REPAYMENT` 资金流水并同步资金账户余额/收益累计。`src/lib/fund-account-ledger-guard.test.ts` 作为回归守门，禁止业务层直接更新资金账户 `balance`、`totalInflow`、`totalOutflow`、`totalProfit`。
